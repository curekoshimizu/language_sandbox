syntax = "proto3";

package foo;

message RequestMsg {
    string prefix = 1;
}

message ResponseMsg {
    string response = 1;
}

service FooService {
    rpc Foo(RequestMsg)
        returns (ResponseMsg);
    // rpc Bar(RequestMsg)
    //     returns (stream ResponseMsg);
}
